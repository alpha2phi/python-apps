version: "3"

services:
  backend:
    build: backend
    restart: always
    ports:
      - 8088:8088
    networks:
      - app_network
    container_name: backend
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./backend:/fastapi

  frontend:
    build: frontend
    restart: always
    depends_on:
      - backend
    ports:
      - 8501:8501
    networks:
      - app_network
    container_name: frontend
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./frontend:/streamlit

networks:
  app_network:
    driver: bridge

volumes:
  data:
    name: data
